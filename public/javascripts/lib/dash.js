// Generated by CoffeeScript 1.6.3
(function() {
  $(function() {
    var $charStats, $completed, $current, $dash, $game, charSource, dashSource, updateChar, updateDash;
    charSource = $('#char-stats').html();
    dashSource = $('#dash-board').html();
    updateChar = Handlebars.compile(charSource);
    updateDash = Handlebars.compile(dashSource);
    $dash = $('#dashBoard');
    $completed = $('#completedQuests');
    $current = $('#currentQuests');
    $charStats = $('#charStats');
    $game = $('#gameAch');
    $current = $('#currentQuests');
    $.get('/charData', {}, function(userCharacter) {
      console.log(userCharacter);
      $dash.html(updateDash(userCharacter));
      return $charStats.html(updateChar(userCharacter));
    });
    $(document).on('click', '.choosePath', function() {
      $('#pathChooser').fadeOut();
      return $.get('/charData', {}, function(userCharacter) {
        $dash.html(updateDash(userCharacter));
        return $charStats.html(updateChar(userCharacter));
      });
    });
    $(document).on('click', '.addQuest', function() {
      $('#currentQuests').append($('<p class="quest">QuestTest</p>'));
      return $('.quest').hallo({
        editable: true
      });
    });
    $(document).on('hallodeactivated', '.quest', function() {
      var quest;
      $(this).fadeOut('fast').fadeIn('fast');
      quest = $(this).text();
      console.log(quest);
      return $.post('/addQuest', {
        currentQuest: quest
      }, function(data) {
        return console.log('datafromhallodeactive', data);
      });
    });
  });

}).call(this);
